import { createContext } from "react";

//Creates a context which can be directly used by different components without prop drilling
const noteContext = createContext();

export default noteContext;
